class main {

    int inc(int x) {
        return x + 1;
    }
    int read_int() extern;
    void print(boolean s) extern;
    void main() {
        int y;
        y = read_int();
        y = inc(y);
        if (y == 1)
            print(y==1);
        else
            print(y!=1);
            
        if(y != 1)
            print(y!=1);
            
        while y == 1 return;   
        
        for y = 3, y+1
            return;
            
        break; continue;   
        
        } 
    }
class main2 { 
//Clase vacia
}


//Prueba de lista de clases, metodos, declaracion de variables, estructuras de control, etc.
 
